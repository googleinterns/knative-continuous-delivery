apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: knative-continuous-delivery
  namespace: default
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/minScale: "1"
    spec:
      containers:
        - image: gcr.io/yifeiexperiment/knative-continuous-delivery:v1
